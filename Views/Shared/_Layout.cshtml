@{
    bool isLoggedIn = true;   // replace later with real auth

    var currentController = ViewContext.RouteData.Values["controller"]?.ToString();
    var currentAction = ViewContext.RouteData.Values["action"]?.ToString();

    bool isAdminPage = currentController == "Admin";

    Func<string, string, string> isActive = (controller, action) =>
    {
        return currentController == controller && currentAction == action ? "active" : "";
    };
}
<!DOCTYPE html>
<html>
<head>
    <title>BookShelf</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="stylesheet" href="~/css/site.css" />
    @RenderSection("Styles", required: false)

    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">
</head>

<body>

    @if (!isAdminPage)
    {
        <!-- ================= HEADER ================= -->
        <div class="header">
            <div class="header-left">
                <img src="~/images/logo.png" class="logo-img" />
                <span>BookShelf</span>
            </div>

            <div class="header-right">
                <div class="search-box">
                    <div class="search-box">
                        <input type="text" id="globalBookSearch" placeholder="Search books..." />
                        <i class="fa fa-search"></i>
                    </div>
                    <i class="fa fa-search"></i>
                </div>

                @if (isLoggedIn)
                {
                    <i class="fa fa-bell header-icon"></i>
                    <i class="fa fa-user header-icon"></i>
                }
                else
                {
                    <a href="/Account/Login" class="header-link">Login</a>
                    <a href="/Account/Register" class="header-link">Sign up</a>
                }
            </div>
        </div>
    }

    <!-- ================= MAIN ================= -->
    <div class="main-container">

        @if (!isAdminPage)
        {
            <!-- ================= SIDEBAR ================= -->
            <div class="sidebar">
                <ul>

                    <li class="@isActive("Home", "Index")">
                        <a asp-controller="Home" asp-action="Index">
                            <i class="fa fa-home"></i>
                            <span>Dashboard</span>
                        </a>
                    </li>

                    <li class="@isActive("Home", "Category")">
                        <a asp-controller="Home" asp-action="Category">
                            <i class="fa fa-th-large"></i>
                            <span>Category</span>
                        </a>
                    </li>

                    @if (isLoggedIn)
                    {
                        <li class="@isActive("Order", "Cart")">
                            <a asp-controller="Order" asp-action="Cart">
                                <i class="fa fa-cart-shopping"></i>
                                <span>Cart</span>
                            </a>
                        </li>

                        <li class="@isActive("Order", "Orders")">
                            <a asp-controller="Order" asp-action="Orders">
                                <i class="fa fa-box"></i>
                                <span>My Orders</span>
                            </a>
                        </li>

                        <li class="@isActive("Ebooks", "Index")">
                            <a asp-controller="Ebooks" asp-action="Index">
                                <i class="fa fa-book"></i>
                                <span>E-Books</span>
                            </a>
                        </li>

                        <li>
                            <a href="#">
                                <i class="fa fa-heart"></i>
                                <span>Wishlist</span>
                            </a>
                        </li>

                        <li>
                            <a href="#">
                                <i class="fa fa-upload"></i>
                                <span>Upload PDF</span>
                            </a>
                        </li>

                        <li>
                            <a href="#">
                                <i class="fa fa-book-open"></i>
                                <span>Upload Book</span>
                            </a>
                        </li>

                        <li>
                            <a href="#">
                                <i class="fa fa-info-circle"></i>
                                <span>About</span>
                            </a>
                        </li>

                        <li>
                            <a href="#">
                                <i class="fa fa-right-from-bracket"></i>
                                <span>Logout</span>
                            </a>
                        </li>
                    }
                    else
                    {
                        <li>
                            <a asp-controller="Account" asp-action="Login">
                                <i class="fa fa-cart-shopping"></i>
                                <span>Cart</span>
                            </a>
                        </li>

                        <li>
                            <a asp-controller="Account" asp-action="Login">
                                <i class="fa fa-heart"></i>
                                <span>Wishlist</span>
                            </a>
                        </li>

                        <li>
                            <a asp-controller="Account" asp-action="Login">
                                <i class="fa fa-book"></i>
                                <span>E-Books</span>
                            </a>
                        </li>
                    }
                </ul>
            </div>
        }

        <!-- ================= CONTENT ================= -->
        <div class="content">
            @RenderBody()
        </div>

    </div>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>